{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h2>Créer un nouveau post!</h2>
        {# This will generate the form #}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}
