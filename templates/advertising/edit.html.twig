{% extends 'base.html.twig' %}

{% block title %}Edit your advert{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="mb-5">Edit your post.</h2>
        {# This will generate the form #}
        {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Send!</button>
        {{ form_end(form) }}
    </div>

{% endblock %}